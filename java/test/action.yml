name: 'MA Java CI Test'
description: 'Java build and test flow'
inputs:
  java-version:
    description: 'The Java version to set up. Takes a whole or semver Java version.'
    required: true
    default: 17
  java-distribution:
    description: 'Java distribution.'
    required: true
    default: 'temurin'
  sentry-environment:
    description: 'Sentry environment for the release (e.g., development, staging, production)'
    required: false
    default: 'production'
runs:
  using: "composite"
  steps:
    - uses: actions/checkout@v6

    - name: Set up JDK
      uses: actions/setup-java@v5
      with:
        java-version: ${{ inputs.java-version }}
        distribution: ${{ inputs.java-distribution }}
        cache: gradle

    - name: Build and analyze
      shell: bash
      run: ./gradlew build

    # Generates the full Gradle dependency tree and pushes it to GitHub
    - name: Generate and submit dependency graph
      uses: gradle/actions/dependency-submission@v5

    # Creates the release in Sentry and associates commits
    - name: Create Sentry Release
      if: github.event_name == 'push'
      uses: getsentry/action-release@v3
      with:
        environment: ${{ inputs.sentry-environment }}
        inject: false # Disables JS sourcemap injection for Java projects
