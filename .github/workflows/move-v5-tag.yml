name: Move v5 tag to v5.1

on:
  workflow_dispatch:

jobs:
  move-tag:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-tags: true
      - name: Move v5 tag to v5.1
        run: |
          git tag -f v5 v5.1
          git push origin v5 --force
